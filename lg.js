let fs = require('fs');
const { off } = require('process');

//fs.readFile('de.json',(err, data) => {
	//if (err) throw err;
	  // let arr = data.toString();
	   //arr = JSON.parse(arr);
	    // for (let i in arr){}})
fs.readdir('html', function (err, file) {
		
		 for (let i=1; i < file.length; i++){
			console.log(file);
		};//ror
		//let arrHT = file[].replace(`{$`+ i + `}`, arr[i]);
	//fs.mkdir('de', () => {});
	//	fs.writeFile('de/index.html',arrHT, (err) => {
	//if (err) {
	 // console.error(err)
	// return
	//}})
	
 // })
	
//})	
	//console.log(file);
	})//readdir		
		 

//fs.readFile('Html/index_key.html',(err, dataHT) => {
	//if (err) throw err;
	   //let arrHT = dataHT.toString();
			
	//for (let i in arr){
	//	arrHT = arrHT.replace(`{$`+ i + `}`, arr[i]);
		
	//};
	//fs.mkdir('de', () => {});
	


	//fs.writeFile('de/index.html',arrHT, (err) => {
	//if (err) {
	 // console.error(err)
	// return
	//}})
	
 // })
//}

//})
